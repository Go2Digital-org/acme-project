APP_NAME="ACME CSR Platform CI"
APP_ENV=testing
APP_KEY=base64:xE+zr0TJC9GxPhgdoo3FgR7cSwuhB90TePOUVKzW710=
APP_DEBUG=true
APP_URL=http://127.0.0.1:8089
DEBUGBAR_ENABLED=false

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file

PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=4

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error

# MySQL Database for CI
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=acme_corp_csr_test
DB_USERNAME=root
DB_PASSWORD=root

# Optimize for CI performance - use array drivers
SESSION_DRIVER=array
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync

CACHE_STORE=array
CACHE_DRIVER=array
CACHE_PREFIX=ci_

# No Redis needed in CI
REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=array
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="ci@acme-corp.com"
MAIL_FROM_NAME="${APP_NAME}"

# Disable external services for CI
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# Payment gateways in test mode
STRIPE_MODE=test
STRIPE_SECRET=sk_test_dummy
STRIPE_PUBLIC=pk_test_dummy
STRIPE_WEBHOOK_SECRET=whsec_test_dummy

PAYPAL_MODE=sandbox
PAYPAL_CLIENT_ID=test_client
PAYPAL_SECRET=test_secret

# Disable monitoring in CI
TELESCOPE_ENABLED=false
PULSE_ENABLED=false
NIGHTWATCH_ENABLED=false
SENTRY_LARAVEL_DSN=

VITE_APP_NAME="${APP_NAME}"

# Disable external connections that could cause hanging
QUEUE_CONNECTION=sync
BROADCAST_CONNECTION=null
MAIL_MAILER=array

# Disable all third-party services
SCOUT_DRIVER=null
SOCIALITE_PROVIDERS_DISABLED=true

# Database optimization for CI
DB_FOREIGN_KEYS=false
DB_STRICT_MODE=false

# Disable notifications during CI
NOTIFICATIONS_ENABLED=false

# Disable cache warming for CI to prevent redirects
CACHE_WARMING_ENABLED=false

# Disable tenancy features for CI
TENANCY_ENABLED=false
TENANCY_CENTRAL_DOMAINS=127.0.0.1,localhost

# CI Performance Settings
TEST_MEMORY_LIMIT=2G
TEST_MAX_EXECUTION_TIME=300

# Disable multi-tenancy for CI browser tests
# CENTRAL_DOMAIN=127.0.0.1:8000
# TENANT_IDENTIFICATION=subdomain
# TENANT_DATABASE_PREFIX=tenant_
# TENANT_DATABASE_SUFFIX=